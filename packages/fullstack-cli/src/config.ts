import path from 'path';

export const apps = ['blog', 'api', 'front', 'front-next'];

export const libs = ['deploy'];
export const rootApps = ['courses'];

export const rootPath = path.join(__dirname, '../../..');
