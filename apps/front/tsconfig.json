{
  "extends": "../../base.tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "baseUrl": ".",
    "noEmit": true
  },
  "references": [],

  "include": ["src", "scripts"],
  "exclude": ["tests", "dist", "build"]
}
